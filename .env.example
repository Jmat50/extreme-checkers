# Copy to .env.local for local production-style testing (optional).
# Leave unset during `npm run dev` — Vite proxies /api and /socket.io to localhost:8000.

# Render web service URL (no trailing slash), e.g. https://extreme-checkers-api.onrender.com
VITE_GAME_SERVER_URL=
